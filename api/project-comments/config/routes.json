{
  "routes": [
    {
      "method": "GET",
      "path": "/project-comments",
      "handler": "project-comments.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-comments/count",
      "handler": "project-comments.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/project-comments/:id",
      "handler": "project-comments.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/project-comments",
      "handler": "project-comments.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/project-comments/:id",
      "handler": "project-comments.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/project-comments/:id",
      "handler": "project-comments.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
